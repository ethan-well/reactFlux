import React from 'react';
import ReactDOM from 'react-dom';

export default class HeaderComponent extends React.Component {
  constructor(props) {
    super(props)
  }
  render() {
    return(
      <header>This is Headr!</header>
    )
  }
}
